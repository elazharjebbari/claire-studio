# Specs techniques par lot — index

| Lot | Titre | Estimation | Dépendances | Fiche |
| --- | --- | --- | --- | --- |
| Lot 0 | Verrouillage du langage couleur (socle transverse) | M — La logique est mécanique et localisée (remplacements 1-pour-1 hex→token, extension d'un composant existant) sans nouvelle architecture d'état ni backend, ce qui pousse vers S. Mais le volume de fichiers touchés (~15), la création de la garde CI + du harnais AA (avec ajustement itératif des valeurs light de la rampe triage jusqu'au vert), la décision de design sur la frontière métier/état du mode Compare, la migration iconographique partielle de SentenceMenu et la nécessité de basculer/tester le thème clair en E2E (couverture aujourd'hui implicite) ajoutent du soin et de la vérification croisée. M réaliste : ~2–3 jours dev + tests, sans dépendance bloquante. | AUCUNE dépendance entrante : Lot 0 est le PRÉREQUIS transverse de tous les autres lots de la refonte atelier (couleurs/contraste = socle). Il doit être livré EN PREMIER., Fondations déjà présentes et réutilisées (pas des dépendances de lot, déjà au code) : tokens sémantiques --sem-* + classes Tailwind tokenisées (tailwind.config.ts), bascule clair/sombre par CSS vars (globals.css), readableTextColor + luminance (tokens.ts), getThemeToken/setRuntimeThemes (H4 schéma tiers), harnais axe E2E (e2e/a11y.spec.ts)., Coordination (non bloquante) : le Lot 'États des boutons' réutilisera l'extension <Button> faite ici ; le Lot 'Système de thèmes (icônes)' réutilisera le champ icon/getThemeToken ; le Lot 'Iconographie' finira la migration Unicode→Lucide entamée a minima dans SentenceMenu. Garder ces extensions additives et rétro-compatibles. | [lot-0.md](lot-0.md) |
| Lot 1 | Cadre dock unifié + plancher de lecture | L — périmètre large et transverse (la décision design le classe Effort L). Touche le contenant de TOUT le flux : refonte d'un composant central contrôlé (ResizablePanels) + 3 primitives nouvelles (CollapseRail, WorkspaceContextBanner, AuxDrawer) + helper pur + extension front ET backend de la persistance par compte + migration localStorage→serveur. Coût dominé par la QA non-régression (drag/clavier/ARIA, replis, overlays, fusion bandeaux, sync serveur multi-poste) et la préservation stricte des nombreux data-testid alimentant des e2e existants. Les sous-tâches sont bien isolables (backend, schéma, store, helper, primitives, câblage) ce qui borne le risque, mais le volume reste un L franc. | Lot 0 (Verrouillage du langage couleur) — REQUIS avant : ce lot tokenise les bandeaux d'état (suppression des amber/slate/sky en dur) et s'appuie sur la garde anti-hex + les tokens sémantiques warning/info/accent validés AA clair/sombre. La garde n'existe pas encore dans le repo actuel (.eslintrc.json sans règle hex, script check-no-hex.mjs absent) → livrer Lot 0 (au moins sa garde + tokens) d'abord., Réutilise (déjà en place, pas à recréer) : la couche prefs PAR COMPTE (store/prefs.ts, schema.ts, useUiPrefsSync, PATCH /me), la primitive Disclosure et les conventions de rail/repli existantes dans ResizablePanels, lucide-react., AUCUNE dépendance aux lots 2-9 : Lot 1 est l'infrastructure ; les presets de phase (greffe ultérieure) et le registre de raccourcis (Lot 9) viendront au-dessus sans bloquer ce lot. | [lot-1.md](lot-1.md) |
| Lot 2 | Système d'états de boutons unifié | M — Le cœur (étendre &lt;Button&gt; + 2 helpers purs) est petit et bien cadré (tokens et store déjà en place, ~S). Ce qui fait basculer en M, c'est la migration transverse mécanique mais nombreuse : ~25 boutons sur 5 fichiers (SelectionToolbar/BlockToolbar, QuickActionRail, WorkspaceToolbar, SubmissionProgressDialog), la chasse aux hex/glyphes Unicode, la préservation stricte des data-testid, et la double passe a11y clair/sombre + e2e pending/erreur. Pas de backend, pas de schéma de store nouveau, donc pas de risque L. Bornable et largement testable. | Lot 0 (prérequis transverse) — Garde anti-hex + lint token-only (frontend/scripts/check-no-hex.mjs, frontend/.eslintrc.json, frontend/src/lib/tokens.ts) : aujourd'hui ABSENTS du repo. Sans la garde CI, les emerald-*/red-* re-fuient ; ce lot DOIT s'appuyer dessus pour verrouiller le résultat. Les tokens sémantiques --sem-* eux sont DÉJÀ présents (globals.css + tailwind.config.ts), donc le câblage couleur ne bloque pas., Coordination (souple) avec le Lot couleurs/iconographie pour les tokens MÉTIER triage (--triage-c1..c5) et thème consommés par QuickActionRail : le bouton triage retire son hex inline mais sa couleur reste métier ; si --triage-* n'est pas encore livré, prévoir un stub token-aware temporaire (pas de blocage dur)., Indépendant des lots barre d'outils (WorkspaceToolbar peut être migré ici sans attendre sa refonte de structure) et raccourcis (Lot 9) : aucune dépendance bloquante. Le helper saveStateToButton réutilise le store autosave existant (aucune dépendance à un autre lot). | [lot-2.md](lot-2.md) |
| Lot 3 | Icônes de thème monochromes + assainissement du chip | M — Le helper pur + le wrapper ThemeGlyph sont rapides (S). Le coût réel est (a) le choix design des 20 glyphes univoques et mémorisables (validation daltonienne incluse), (b) la substitution propre aux 4 sites de rendu sans casser les data-testid ni les ~470 tests existants, (c) l'assainissement des 2 hex avec assertions. Pas de backend, pas de store, pas de migration → reste contenu. M et non L car aucune logique d'état ni d'API, périmètre front isolé et fortement testé. | Lot 0 — Garde anti-hex + lint token-only (PRÉREQUIS TRANSVERSE, NON encore livré : ni frontend/scripts/check-no-hex.mjs ni la conf eslint token-only n'existent). L'assainissement des 2 hex de ClauseChip doit s'aligner sur les tokens/variables CSS définis par Lot 0 ; à défaut, faire l'assainissement avec rgb(var(--sem-success)) et classes Tailwind existantes (text-info/border-info déjà présentes) et couvrir par un test unit en attendant la garde CI., Registre thème→icône (fondation transverse annoncée : getThemeToken + champ icon, lib/themeIcons.ts) — c'est précisément CE lot qui le matérialise ; aucune dépendance amont autre que lucide-react (déjà présent)., Coordination (non bloquante) avec Lot 4 (zone Document / bord gauche hiérarchisé) pour le rail coloré du DocumentPanel : si le rail adopte aussi l'icône monochrome, mutualiser getThemeIcon/ThemeGlyph plutôt que dupliquer. Le périmètre ClauseChip de ce lot n'attend pas Lot 4. | [lot-3.md](lot-3.md) |
| Lot 4 | Plan virtualisé filtrable + sortie des Overlays | M — Le cœur fonctionnel (planCoverage pur, filtres, Disclosure enveloppant un fieldset existant, purge tokens) est de complexité modérée et à faible risque. Ce qui pousse vers le haut de M : (a) l'introduction d'une lib de virtualisation tierce (nouvelle dépendance) avec son piège jsdom/tests et la réécriture de la synchro chip→vue ; (b) l'adaptation de plusieurs specs E2E existantes qui touchent les toggles déplacés sous le Disclosure ; (c) la persistance par compte (schema/migratePrefs/round-trip /me). Pas de L car aucun backend métier, modèle mental préservé, et le fieldset/legend a11y est réutilisé tel quel. | Lot 0 (Garde anti-hex + lint token-only + primitive Disclosure) : la primitive <Disclosure> et la garde check-no-hex.mjs sont des prérequis. Disclosure.tsx n'existe PAS encore dans le repo → soit Lot 0 la livre avant, soit ce lot la crée (à arbitrer pour éviter le double travail)., Lot thèmes→icônes (getThemeToken + champ icon / themeIcons.ts) : utile pour le sélecteur de filtre par thème (glyphe monochrome) et la purge d'identité. getThemeToken existe déjà (tokens.ts) ; themeIcons.ts n'existe PAS encore → si le filtre par thème veut l'icône, dépendre de ce lot ou se contenter de la pastille/label en attendant., Lot Persistance de layout PAR COMPTE (extension du store prefs) : la mécanique prefs (schema/migratePrefs/useUiPrefsSync/PATCH /me) est réutilisée pour planFilter + displayOpen. Déjà en place dans le repo (schema.ts/prefs.ts) — dépendance SATISFAITE, on l'étend., Indépendant du Lot Layout 3-panneaux (le repli en rail du panneau Plan est explicitement HORS périmètre de cette variante retenue — la décision le note ; il viendra avec le lot Layout). | [lot-4.md](lot-4.md) |
| Lot 5 | Menu contextuel express (retrait LLM) | M — Le cœur (retirer la section LLM + réordonner + tokeniser + relucider) est mécanique et localisé à un seul composant, sans toucher au store ni à l'API. Ce qui pousse vers M plutôt que S : (1) la mise à jour de 3 specs e2e qui dépendent explicitement du bloc LLM (document-ux, llm-compare, theme-multipicker) avec re-câblage de l'adoption sur le chemin clavier ; (2) la gestion fine du repli/focus a11y (Disclosure éventuellement non livrée → repli local propre) ; (3) la préservation chirurgicale des types exportés et des indices discrets pour ne pas casser BoundaryEvidence/RationaleHover/Compare. Pas de L : aucune migration de données, aucun nouveau flux, primitives largement réutilisées. | Lot 0 (Garde anti-hex + lint token-only + tokens sémantiques) : prérequis pour que bg-success/warning soient validés par la CI. Les tokens sont déjà exposés dans tailwind.config.ts (vérifié), donc ce lot peut avancer même si la garde CI complète n'est pas finalisée — mais idéalement Lot 0 livré avant., Primitive <Disclosure> (fondation transverse) : ABSENTE du repo à ce jour. Dépendance souple — si non livrée, repli local toggle aria-expanded/aria-controls tokenisé, à remplacer ensuite. Préférer la primitive livrée avant pour éviter la dette., Indépendant du Lot 'États des boutons' (Button state-machine) : Valider/Traduire peuvent être tokenisés directement sans attendre les variantes success/danger de <Button> ; migration ultérieure vers <Button variant=success/danger> = suivi cosmétique non bloquant., Pas de dépendance sur Lot Inspecteur ni Lot Arbitrage C1→C5 : l'arbitrage LLM retiré du menu vit déjà ailleurs (clavier 1/2, œil de frontière, Compare), surfaces en place AUJOURD'HUI ; ce lot ne crée pas de trou fonctionnel nécessitant l'inspecteur refondu au préalable. | [lot-5.md](lot-5.md) |
| Lot 6 | Inspecteur en accordéon + arbitrage ambiant C1→C5 | L — La tokenisation seule (étape 1-2) est M, mais la refonte structurelle de l'inspecteur en accordéon avec persistance par compte, l'arbitrage ambiant câblé sur useTriage, la correction de l'action Adopter (atomicité d'undo) et la couverture de tests (unit + composant + e2e axe clair/sombre) en font un lot L. Le risque dépendances (Disclosure/Button absents) peut le faire glisser si les fondations ne sont pas prêtes. | Lot 0 — Garde anti-hex + lint token-only (check-no-hex.mjs, tokens.ts) : PRÉREQUIS transverse, doit précéder la tokenisation de levels.ts., Lot fondation <Disclosure> — Disclosure.tsx ABSENT actuellement : bloquant pour l'accordéon. Doit être livré avant l'étape 4., Lot États-des-boutons <Button> — Button.tsx ABSENT actuellement : bloquant pour Valider/Supprimer machine d'états. Doit être livré avant l'étape 4., Lot fondation Persistance de layout PAR COMPTE (prefs/schema.ts, useUiPrefsSync) : requis pour mémoriser l'ouverture des sections (étape 5) — partiellement présent (inspectorOpen existe déjà)., Lot Système de thèmes (thème→icône) : pas bloquant mais cohérent — ClauseChip/badges déjà câblés sur getThemeToken., Indépendant des lots Plan/ToC, Overlays, Zone Document, Menu contextuel, Barre d'outils (pas de couplage de code direct, seulement l'état partagé useTriage déjà mutualisé avec la File de triage). | [lot-6.md](lot-6.md) |
| Lot 7 | Gouttière d'arbitrage unifiée (zone document) | L — Périmètre large sur le composant le plus hypertrophié et fragile du produit (DocumentPanel ~1530 lignes) + 2 composants satellites, touchant simultanément couleur (tokenisation transverse), structure (hiérarchie bord gauche), et comportement d'affichage (gouttière opt-in). La logique métier reste figée, ce qui borne le risque, mais la surface de test est importante (préserver ~tous les data-testid de la zone, prouver AA en clair ET sombre, vérifier les flux compare/adoption). Plusieurs sous-tâches sont parallélisables (tokens.ts, ModelBoundaryRail, BoundaryEvidence) mais la consolidation gutter + cheat-sheet + bord gauche concentre le risque sur DocumentPanel. | Lot 0 (prérequis transverse) : garde anti-hex + lint token-only + tokens.ts/design-tokens.json — INDISPENSABLE avant de tokeniser, sinon le check-no-hex CI n'est pas en place., Fondation 'Registre de thème→icône Lucide' (themeIcons.ts + champ icon sur getThemeToken) — requise pour injecter le glyphe monochrome dans clause-badge et BoundaryEvidence (sinon fallback point coloré). À merger avant l'étape 7., Fondation 'Primitive <Disclosure>' + 'Registre de raccourcis unifié' (<Kbd>) — requises pour GestureCheatSheet (révélation progressive + tooltips kbd focusables, fin des title= natifs)., Couplage souple avec Lot 8 (Arbitrage C1–C5) : Lot 7 NE touche PAS les hex de triage/levels.ts ; il prépare seulement le terrain (gouttière/œil unique, tokens). À séquencer en bonne entente pour éviter le chevauchement sur le rendu triage du bord gauche., Lot 9 (raccourcis) déjà partiellement consommé via le registre — le cheat-sheet documente les gestes mais ne les implémente pas ; pas de blocage dur. | [lot-7.md](lot-7.md) |
| Lot 8 | Barre d'outils épurée + tiroir | M — surface d'un seul gros fichier (WorkspaceToolbar ~615 lignes) + un popover, sans changement backend ni store workspace. Le travail est large (3 zones, tiroir, migration Button, tokenisation, anti-Unicode, a11y) mais mecanique et bien borne. Le M (vs S) tient au nombre de testids e2e a preserver (5 specs : lock/triage/annotate/collab-versioning/prefill), a la dependance sur 3 primitives transverses possiblement a creer (Disclosure/Kbd/Button-warning), et au double passage a11y clair+sombre. Le L est evite en gardant les actions critiques hors du tiroir et en montant le contenu plie (zero reecriture e2e). | Lot 'Etats des boutons' (Button as State Machine, primitives.tsx/Button.tsx) — necessaire pour les variantes semantiques warning/danger/success/neutral consommees ici ; a defaut, ajouter au minimum la variante 'warning' tokenisee., Lot Disclosure (primitive de revelation progressive) — coeur du tiroir « Affichage/Outils » ; ABSENT du repo a ce jour., Lot 0 'Garde anti-hex + lint token-only' — prerequis transverse ; le check-no-hex.mjs est ABSENT, donc la tokenisation faite ici n'est pas verrouillee en CI tant que Lot 0 n'est pas livre., Lot 9 'Registre de raccourcis clavier unifie' (shortcuts.ts/Kbd.tsx) — pour les tooltips kbd focusables et le <Kbd> (⌘S) ; partiellement present (useShortcuts.ts existe, Kbd.tsx/shortcuts.ts absents). Degradable : afficher le raccourci en clair token-only si Lot 9 non livre., Lot 'Registre de theme->icone Lucide' (themeIcons.ts) — non bloquant pour la barre (pas de chip ici), mais aligne l'iconographie Lucide unique. | [lot-8.md](lot-8.md) |
| Lot 9 | Mode rafale clavier-first | M — Le geste cœur (valider+suivant souris, curseur collant) et les callbacks existent déjà ; l'essentiel est de créer le registre central + 2-3 primitives (Kbd/Tooltip/ShortcutHelpModal) et de re-router deux hooks sans casser la collision n/1/2 ni les data-testid. Le risque principal est l'intégration (collision de hooks, parité scroll souris/clavier, tokenisation AA des niveaux) plutôt que le volume de code. Pas de backend. Pas L car aucune réécriture de flux, on s'appuie sur l'existant ; pas S car nouvelles primitives transverses + refonte de deux hooks globaux + purge hex/Unicode + couverture vitest/e2e a11y conséquente. | Lot 0 (Garde anti-hex + lint token-only + lib/tokens.ts) : PRÉREQUIS DUR — la purge des hex de TRIAGE_LEVEL_META et la tokenisation du QuickActionRail s'appuient sur les tokens sémantiques et la garde CI. Probablement déjà livré (présent dans les fondations)., Helper readableTextColor (token-aware AA) : requis pour valider le contraste des badges de niveau en clair ET sombre. Déjà existant (lib/tokens.ts)., Registre thème→icône Lucide (themeIcons.ts) : modèle réutilisé pour getTriageLevelLucideIcon ; déjà livré., Recommandé AVANT (non bloquant strict) : Lot 8 (Barre d'outils épurée + tiroir Affichage/Outils) pour héberger l'entrée discrète d'ouverture de la modale '?' sans réintroduire un panneau permanent. Si Lot 8 pas prêt, exposer '?' uniquement au clavier + un bouton minimal., Primitive <Disclosure> (annoncée Lot transverse, fichier ABSENT à ce jour) : utile si le bandeau rafale optionnel est livré en révélation progressive ; non bloquant pour le cœur du lot. | [lot-9.md](lot-9.md) |


> Matrice de tests consolidée : [`00-matrice-tests.csv`](00-matrice-tests.csv).
