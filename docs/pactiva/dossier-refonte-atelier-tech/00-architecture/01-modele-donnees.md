# Modèle de données (store & types)

| Entité | Forme | Note |
| --- | --- | --- |
| WorkspaceState (store Zustand, état d'interaction temps réel) | Champs clés : annotationId, nSentences, focusedSentence, selectedClauseId, draftClauses: DraftClause[], ghostClauses, readOnly, dirty, undoStack/redoStack (DraftClause[][] borné à 200), actionLog (ActionEntry[] borné à 200). Overlays togglables : showUnfairness, ghostJudges (Record<string,boolean>), displayLang ('orig'\|'both'\|'fr'), showBoundaries, showTriageLevels, showQuickActions. Source/comparaison : llmSource (LlmSource), llmVersion, showComparePanel, showAttribution, prefilledJudge. Sélections : selectedSentences:number[], selectedClauseIds:string[]. Mutateurs gardés readOnly (no-op), miroir theme↔themes maintenu (withPrimaryTheme/sanitizeThemeSet), atomicité d'undo par lot. | NE PAS dupliquer l'état de layout/overlays persisté ici : les défauts d'overlays viennent du store prefs (init.overlays), llmSource est transitoire (non ponté). Lots à venir consomment ce store SANS le démanteler (instruction design : 'sert la révélation progressive sans démanteler le store'). showTriageLevels devient secondaire car l'arbitrage C1→C5 migre dans l'inspecteur (Lot 6) ; showBoundaries alimente l'œil de frontière unique (Lot 7). |
| DraftClause (clause en cours d'édition) | localId (string, =clientOpId, jamais numérique), serverId?, anchorIndex, theme (scalaire = miroir du primaire), legalNature\|null, evidenceSpan, rationale, certainty: Certainty\|null, seededFrom? ('preannotation:<judge>'), resolvedFrom? (juge adopté), validated?, themes?: ThemeTag[] (1 primaire + N secondaires), boundary?: {type: BoundaryKind, support:number}, triageLevel?: TriageLevel\|null. | Invariant fort : theme === themes.primary.label, refuge jamais secondaire (sanitizeThemeSet, miroir backend, sinon PATCH 400). 'Reprendre dans mon annotation' (Lot 6) doit DÉSORMAIS copier theme + legalNature en plus de evidenceSpan + rationale. reconcileServerClause aligne sans dirty/undo (anti-diff-perpétuel de la soumission). Re-thème humain purge triageLevel (décision devient humaine, pas moteur). |
| UiPrefsV1 (préférences par compte, store prefs + serveur) | { overlays: UiPrefsOverlays (showUnfairness, displayLang, llmSource…), ghostJudges: Record<string,boolean>, panels: UiPrefsPanels (inspectorOpen, historyOpen, commentsOpen, triageOpen), prefill: {enabled, judge} }. Cache localStorage claire.prefs::<uid>, migratePrefs(raw) pour la compat, rev incrémenté uniquement sur geste utilisateur. | À ÉTENDRE (Lot 1) avec l'état de dock : repli bilatéral (tocCollapsed, inspectorCollapsed), largeurs de panneaux, présentation drawer overlay des auxiliaires. Toute extension passe par migratePrefs + setters idempotents (anti-écho PATCH lors du semis init()). Couche DISJOINTE de useUiStore (shell : theme dark/light, zoom) — aucune réconciliation. |
| ThemeToken / tokens sémantiques (couleur, source unique) | ThemeToken { code, label, color, order } hydraté au runtime depuis le schéma API (setRuntimeThemes, généricité corpus tiers H4) avec repli statique design-tokens.json. À ÉTENDRE d'un champ icon (Lucide, Lot 3). Tokens d'ÉTAT en CSS vars RGB (--sem-* / surfaces), exposés Tailwind en rgb(var() / <alpha-value>). TRIAGE_LEVEL_META et LLM_JUDGES identityColor à migrer en tokens (--triage-c1..c5, --judge-*). | Frontière formalisée NON négociable : un token de thème (métier) ne sert JAMAIS de succès/alerte (état). readableTextColor garantit l'AA des fonds dynamiques. Toute couleur passe par tokens.ts/design-tokens.json — la garde anti-hex l'impose en CI. |
