import { PaperResults } from "@/features/paper/PaperResults";

/** Résultats de la campagne expérimentale finale, organisés par question de recherche. */
export default function PaperResultsPage() {
  return <PaperResults />;
}
