Django>=5.0,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-filter>=24.1
drf-spectacular>=0.27
djangorestframework-camel-case>=1.4
django-environ>=0.11
PyYAML>=6.0
psycopg[binary]>=3.1  # driver Postgres (prod ; dev/test en SQLite)
# realtime (chantier D) — WebSocket via Channels.
channels>=4.0
channels-redis>=4.2  # prod : couche Redis (InMemory en dev/test)
daphne>=4.0          # serveur ASGI (prod/dev) ; non requis pour les tests
# dev/test
pytest>=8.0
pytest-django>=4.8
pytest-asyncio>=0.23
factory-boy>=3.3
hypothesis>=6.0  # tests property-based (batterie multi-annotation)
reportlab>=4.2   # rapports PDF Analysis Lab
ruff>=0.4

# Chiffrement des identifiants de calcul (claire/lab/crypto.py) — sans lui, la
# plateforme REFUSE de stocker un identifiant Grid'5000 plutôt que de l'écrire en clair.
cryptography>=42.0

# Client Grid'5000 (claire/lab/g5k_client.py) — python-grid5000 est le client Python
# référencé par la doc officielle (retry intégré, hiérarchie d'exceptions, HATEOAS),
# préféré à une réimplémentation maison des appels HTTP bruts.
requests>=2.28
python-grid5000>=1.2
